import React from 'react';

const chefRecipe = (props) => {
    const {name, ingredients, cooking_method, rating} = props.recip;
    console.log(props.recip)

    return (
        <div>


            
        </div>
    );
};

export default chefRecipe;